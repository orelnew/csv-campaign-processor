# CSV Campaign Processor Environment Configuration

# URL Shortener Service
# For development (when URL shortener is running locally):
# SHORTENER_URL=http://localhost:8888

# For production (when URL shortener is deployed):
# SHORTENER_URL=https://your-deployed-shortener.netlify.app

# Current default (fallback):
SHORTENER_URL=https://websites-links.netlify.app